#include<iostream>
using namespace std;
main()
{
    int megabytes;
    int bits;
    cout<<" enter the megabytes"<<endl;
    cin>>megabytes;
    bits=megabytes*1024*1024*8;
    cout<<" the toatal bits are"<<bits;
}