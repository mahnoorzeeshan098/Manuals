#include<iostream>
using namespace std;
main()
{
    int array[5];
    for(int ind=0;ind<5;ind++)
    {
        cout<<"Enter the num";
        cin>>array[4];
    }
    cout<<"The first number of array is="<<array[0];
    cout<<endl<<"The last number of array is="<<array[4];

}